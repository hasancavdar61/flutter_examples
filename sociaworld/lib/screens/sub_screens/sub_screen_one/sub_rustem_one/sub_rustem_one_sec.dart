import 'package:flutter/material.dart';

class RustemScreenTwo extends StatelessWidget {
  const RustemScreenTwo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('RüstemScreenTwo'),
    );
  }
}
